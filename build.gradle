plugins {
    id 'org.springframework.boot' version '1.4.3.RELEASE'
    id 'java'
    id 'eclipse'
}


archivesBaseName = 'spring-demo'
version = '0.0.1-SNAPSHOT'

repositories {
    jcenter()
}

dependencies {
    compile("org.springframework.boot:spring-boot-starter-web")
}


apply plugin: 'groovy'
apply plugin: 'java'
apply plugin: 'maven-publish'
apply plugin: "base"
apply plugin: "maven"


def repoUrl = "http://192.168.1.222:8081/repository/SAMPLE-SNAP/"

version = '1.0.0'
group = 'com.firstfewlines.sample'

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: "http://192.168.1.222:8081/repository/SAMPLE-SNAP/") {
                authentication(userName: "admin", password: "admin123")
                pom.groupId = "com.beesightsoft.caf"
                pom.artifactId = "rx"
                pom.version = '2.0.0'
            }
        }
    }
}






